{
  "name": "telegram-web-bridge",
  "version": "1.0.0",
  "description": "Cross-platform bridge between a website chat UI and a Telegram bot",
  "private": true,
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "dev":   "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/kv": "^1.0.0"
  },
  "devDependencies": {
    "vercel": "^35.0.0"
  }
}
